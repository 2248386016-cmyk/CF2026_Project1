# Interactive research site

Open `index.html` directly in a browser. It has no external dependencies and works offline.

Figures are frozen from `outputs/backtest/weekly_vs_monthly/02_multi_factor_comparison.csv` and `outputs/backtest/portfolio_risk_extension/02_key_results.csv`.
